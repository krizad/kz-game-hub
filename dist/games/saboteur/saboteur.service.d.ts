import { RoomState, SaboteurState, SaboteurTool } from '@repo/types';
import { PrivateStateService } from '../private-state.service';
export declare class SaboteurService {
    private readonly privateState;
    private readonly logger;
    constructor(privateState: PrivateStateService);
    private shuffleArray;
    private getRole;
    private setRole;
    private getHand;
    private setHand;
    private getPeekedGoals;
    private getDeck;
    private setDeck;
    private getGoldDeck;
    private setGoldDeck;
    private getGoalContents;
    private setGoalContents;
    private clearRoundPrivateData;
    private buildDrawPile;
    private isMember;
    private drawCard;
    private syncHandSizes;
    private connectedPlayerIds;
    private assignRoles;
    private buildInitialBoard;
    private initPlayers;
    private startRound;
    startGame(room: RoomState, requesterId: string): RoomState | null;
    private advanceTurn;
    private checkExhaustionEnd;
    placePath(room: RoomState, playerId: string, cardIndex: number, x: number, y: number, rotation: 0 | 180): RoomState | null;
    playAction(room: RoomState, playerId: string, payload: {
        cardIndex: number;
        targetPlayerId?: string;
        repairTool?: SaboteurTool;
        goalIndex?: number;
        targetX?: number;
        targetY?: number;
    }): RoomState | null;
    private applyAction;
    discard(room: RoomState, playerId: string, cardIndex: number): RoomState | null;
    autoPass(room: RoomState, playerId: string): RoomState | null;
    private afterPlayResolved;
    private beginGoldPick;
    pickGold(room: RoomState, playerId: string, poolIndex: number): RoomState | null;
    private endRoundSaboteursWin;
    private finalizeRound;
    nextRound(room: RoomState, requesterId: string): RoomState | null;
    reset(room: RoomState, requesterId: string): RoomState | null;
    handlePlayerDisconnect(room: RoomState, socketId: string): void;
    remapSocketId(state: SaboteurState, oldSocketId: string, newSocketId: string): void;
}
