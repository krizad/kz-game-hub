"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __exportStar = (this && this.__exportStar) || function(m, exports) {
    for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.prisma = void 0;
const client_1 = require("@prisma/client");
const pg_1 = require("pg");
const adapter_pg_1 = require("@prisma/adapter-pg");
const adapter_mariadb_1 = require("@prisma/adapter-mariadb");
const dotenv_1 = require("dotenv");
const fs_1 = require("fs");
const path_1 = require("path");
const envCandidates = [(0, path_1.resolve)(__dirname, '../../../.env'), (0, path_1.resolve)(__dirname, '../../.env')];
const envPath = envCandidates.find((p) => (0, fs_1.existsSync)(p));
if (envPath)
    (0, dotenv_1.config)({ path: envPath });
__exportStar(require("@prisma/client"), exports);
const connectionString = `${process.env.DATABASE_URL}`;
const isMysql = /^(mysql|mariadb):\/\//i.test(connectionString);
const adapter = isMysql
    ? new adapter_mariadb_1.PrismaMariaDb(connectionString)
    : new adapter_pg_1.PrismaPg(new pg_1.Pool({ connectionString }));
const globalForPrisma = globalThis;
exports.prisma = globalForPrisma.prisma ??
    new client_1.PrismaClient({
        adapter,
        log: ['query'],
    });
if (process.env.NODE_ENV !== 'production')
    globalForPrisma.prisma = exports.prisma;
