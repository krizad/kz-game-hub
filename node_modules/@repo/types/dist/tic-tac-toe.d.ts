export type TicTacToeCell = 'X' | 'O' | null;
export interface TicTacToeState {
    board: TicTacToeCell[];
    playerXId?: string;
    playerOId?: string;
    currentTurn: 'X' | 'O';
    winner?: 'X' | 'O' | 'DRAW';
    winningLine?: number[];
}
