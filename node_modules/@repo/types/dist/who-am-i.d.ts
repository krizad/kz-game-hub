export type WordMode = 'HOST_INPUT' | 'RANDOM' | 'PLAYER_INPUT' | 'AI_GENERATED';
export type GameActionType = 'SUBMIT_GUESS' | 'VOTE_GUESS' | 'END_TURN' | 'GUESS_WORD' | 'NEXT_TURN' | 'END_MATCH';
export type VoteResult = 'YES' | 'NO' | 'MAYBE';
export interface WordCategory {
    name: string;
    count: number;
}
export interface WhoAmIGameState {
    currentTurn: string;
    playerWords: Record<string, string>;
    currentGuess: string | null;
    votes: Record<string, VoteResult>;
    turnStatus: 'THINKING' | 'VOTING' | 'RESULT';
    guessResult?: boolean;
    guessedWord?: string;
    winner: string | null;
    currentRound: number;
    maxRounds: number;
    eliminatedPlayers: string[];
    phase: 'COLLECTING_WORDS' | 'AWAITING_HOST_INPUT' | 'ASKING' | 'FINAL_GUESS';
    finalGuessUsed: string[];
    wordSubmissions?: Record<string, string>;
    wordSubmissionCategory?: string;
}
