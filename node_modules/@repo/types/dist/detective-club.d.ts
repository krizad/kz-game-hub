export declare enum DetectiveClubPhase {
    SETUP = "SETUP",
    PLAYING_ROUND_1 = "PLAYING_ROUND_1",
    PLAYING_ROUND_2 = "PLAYING_ROUND_2",
    DISCUSSION = "DISCUSSION",
    VOTING = "VOTING",
    SCORING = "SCORING"
}
export declare enum DetectiveClubRole {
    INFORMER = "INFORMER",
    CONSPIRATOR = "CONSPIRATOR",
    DETECTIVE = "DETECTIVE"
}
export interface DetectiveClubPlayer {
    id: string;
    role: DetectiveClubRole;
    score: number;
    hand: string[];
    playedCards: string[];
    votedFor: string | null;
}
export interface DetectiveClubState {
    currentPhase: DetectiveClubPhase;
    informerId: string | null;
    conspiratorId: string | null;
    word: string | null;
    activePlayerId: string | null;
    playOrder: string[];
    round1StarterId: string | null;
    scoreDeltas?: Record<string, number>;
    deck?: string[];
    discardPile?: string[];
    players: Record<string, DetectiveClubPlayer>;
}
