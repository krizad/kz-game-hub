export type RPSChoice = 'ROCK' | 'PAPER' | 'SCISSORS';
export interface RPSState {
    activePlayers: string[];
    queue: string[];
    choices: Record<string, RPSChoice>;
    scores: Record<string, number>;
    roundWinner?: string | string[];
    gameWinner?: string | string[];
}
